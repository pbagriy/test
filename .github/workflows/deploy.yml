name: Deploy

on:
  workflow_dispatch:
    inputs:
      check:
        required: true
        description: dunno
        type: boolean

jobs:
  deploy:
    uses: ./.github/workflows/conditions-test.yml
    with:
      check: ${{ inputs.check }}
    secrets: inherit
